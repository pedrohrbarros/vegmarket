import { useTranslations } from 'next-intl';
import { motion } from 'framer-motion'

export default function RegisterForm() {

  const t = useTranslations('Authentication');

  return (
    <form className="w-full h-full p-5 bg-forest">

    </form>
  )
}